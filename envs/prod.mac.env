# Production environment for Mac (PostgreSQL)
DEBUG=True
DJANGO_ENV=production
DJANGO_SETTINGS_MODULE=config.settings.production

# Database configuration
DB_ENGINE=django.db.backends.postgresql
DB_NAME=triad_db
DB_USER=lucypatton
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Server configuration - Use Django's runserver for testing
WSGI_SERVER=runserver
WSGI_PORT=8000

# Security settings
SECRET_KEY=django-insecure-prod-key-please-change-this
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
SECURE_SSL_REDIRECT=False

# Static files
STATIC_URL=/static/
MEDIA_URL=/media/ 